[project]
name = "obsidian-dispatch-mcp"
version = "0.1.0"
description = "MCP server that bridges an Obsidian vault with the ruflo dispatch engine"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.4",
    "python-frontmatter>=1.1.0",
]

[tool.uv]
package = false
