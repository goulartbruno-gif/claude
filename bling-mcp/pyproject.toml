[project]
name = "bling-mcp"
version = "0.1.0"
description = "MCP server for the Bling ERP API (OAuth2, v3)"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.4",
    "httpx>=0.28.0",
]

[tool.uv]
package = false
